services:
  payment-service:
    image: ktshonln/payment-service:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: payment-svc
    restart: unless-stopped
    networks:
      - katisha-net
    environment:
      NODE_ENV: production
    env_file:
      - .env
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:8099/health', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

networks:
  katisha-net:
    external: true
